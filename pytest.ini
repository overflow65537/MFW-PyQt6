[pytest]
markers =
    stage1: basic config and tasks
    stage2: config lifecycle (add/select/delete)
    stage3: task lifecycle (add/modify/delete)
    stage4: reorder tasks
    stage5: default config and know_task checks
    stage6: ui-related signals (toggle_task_check)
